import { Text } from 'react-native';
import { contentStyles } from '../App.styles';
import { TeleText } from '../components/TeleText';

export default function Settings() {
    return <TeleText style={contentStyles}>Settings</TeleText>;
}
